// 8. Express Middleware Logging

// Create an Express server.
// Write a middleware that logs the request URL and method every time a route is accessed.
// Routes:

// / → send “Middleware working”

// /test → send “Test route”