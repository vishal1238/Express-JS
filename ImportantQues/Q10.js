// 10. Upload File & Reverse Content (fs + express + POST)

// Create an Express server with an HTML form containing a <textarea>.
// User enters text → submit → server saves it to a file data.txt.
// Then read the file, reverse its contents using JavaScript, and show the reversed text as response.